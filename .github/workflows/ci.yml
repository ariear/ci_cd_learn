name: ci-cd-learn

on: 
  push: 
    branches:
      - main
      
jobs:
  nyapa:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: print hello
        run: echo "Hello CI/CD"
      - name: menampilkan tanggal
        run: date

  deploy:
    needs: nyapa
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Deploying app..."
